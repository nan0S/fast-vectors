#!/bin/sh

if [ "$1" = "static" ]; then
    arg="static"
    shift
fi

if [ "$arg" = "static" ]; then
    ./run-current static_vector_benchmark \
        --push_cons_dest \
        --int_arg=2000 \
        --string_arg=1500 \
        --array_arg=1500 \
        --test_type_arg=1500 \
        $@
else
    ./run-current vector_benchmark \
        --push_cons_dest \
        --common_iters=1 \
        $@
fi